"use client";

function LocaleToggle({ isIcon = false }: { isIcon?: boolean }) {
  // TODO: Temporarily disabled - only English for now
  // To re-enable, restore the component implementation from git history
  return null;
}

export default LocaleToggle;
